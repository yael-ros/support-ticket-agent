# docs/

Screenshots referenced by the top-level `README.md`. Git doesn't track
empty directories, hence this file.

Drop these two in here (exact filenames — the root README already
references them, no further edits needed once they exist):

- **`screenshot-auto-send.png`** — from the [live demo](https://support-ticket-agent-9ptn.onrender.com/demo):
  click "SSO login loop," submit, screenshot the result panel. Reliably
  classifies as `it_support`/high confidence and auto-sends with a
  grounded reply.
- **`screenshot-human-review.png`** — same page, click "Vague complaint,"
  submit, screenshot the result panel. Low-detail tickets typically fall
  below the confidence threshold and route to human review.
